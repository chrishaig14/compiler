import example.person.Person

fun say_hello() -> String {
    return "Hello world!"
}

fun say_goodbye() -> String {
    return "See ya!"
}

fun print_children(p: Person) {
    for c @ p.children {
        print(c.name + " -> " + c.age.str())
    }
}