import core.Integer
import core.print
import core.String
import core.List
import core.Boolean
import core.range
import core.Option
import core.exit

fun same(x: List[t])->t{
    return x.len
}

fun main()->Integer{
//    print(same(2))
//    var x = ["Hello", "world", "I'm", "Chris"]
//    var y = x.len()
//    print(y.str())
    return 0
}